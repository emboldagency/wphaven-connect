dist/*
!dist/.gitkeep
!dist/archives/
!dist/extracted/
dist/archives/*
dist/extracted/*
.idea/

# Composer dependencies (install with: composer install)
# Keep vendor and lock tracked for clone usability

# PHPCS cache
.phpcs.xml.cache